// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("xm", "for xm support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("xm", "enable xm support", "no");

if (PHP_XM != "no") {
	EXTENSION("xm", "xm.c");
}

